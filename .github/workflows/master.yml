name: Deploy Applicant Tracking System

# Controls when the workflow will run
on:
  push:
    branches: [ "master" ]
jobs:
  deploy:    
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
